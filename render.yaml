services:
  - type: web
    name: my-app
    env: node
    plan: free
    buildCommand: |-
      rm -rf dist
      npm install
      npm run build
    startCommand: npm run start
    healthCheckPath: /
